function execute(a, b, operation) {
  return operation(a, b)
}
const add = (num1, num2,) => num1 + mum2
const multiply = (num1, num2) => num1 +num2




function finishAnalysis(name, onDone) {
  console.log("analise concluida");
onDone(name)
}

const test = (name) => {
   console.log(`${name}`)

}








